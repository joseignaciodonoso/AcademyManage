# Netlify configuration for Next.js app
# Docs: https://docs.netlify.com/integrations/frameworks/next-js/overview/

[build]
  command = "npm run build:netlify"
  publish = ".next"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = []

# Next.js plugin enables routing, image optimization, SSR/ISR on Netlify
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Optional: common Next.js envs (set actual values in Netlify UI or env file)
#[[build.environment]]
#  NEXT_TELEMETRY_DISABLED = "1"
#  NODE_VERSION = "18"

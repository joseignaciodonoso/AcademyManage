# Copy this file to `.env.local` for local development.
# Do NOT commit `.env.local`.

# PostgreSQL connection string (Neon recommended). Must include sslmode=require
# Example (pooled):
# postgresql://USER:PASSWORD@your-project-pooler.region.aws.neon.tech/DBNAME?sslmode=require&channel_binding=require
DATABASE_URL=

# NextAuth secret used to sign/encrypt tokens. Any long random string.
# You can generate one with: `openssl rand -base64 32`
NEXTAUTH_SECRET=

# Public base URL of your app.
# For local Netlify CLI dev, use http://localhost:8888
# For local Next.js dev, use http://localhost:3001
NEXTAUTH_URL=http://localhost:3001
NEXT_PUBLIC_BASE_URL=http://localhost:3001

# === Unified Checkout Providers ===
# Mercado Pago
MERCADOPAGO_ACCESS_TOKEN=

# Khipu
KHIPU_RECEIVER_ID=
KHIPU_SECRET=

# Webpay (Transbank)
WEBPAY_COMMERCE_CODE=
WEBPAY_API_KEY=

# Transferencia (datos visibles para el alumno)
TRANSFER_BANK_NAME=
TRANSFER_ACCOUNT=
TRANSFER_RUT=
TRANSFER_EMAIL=

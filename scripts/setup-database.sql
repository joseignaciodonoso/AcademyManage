-- Setup script for martial arts academy database
-- This script will be run to initialize the database schema

-- Enable UUID extension if using PostgreSQL
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- The actual schema will be created by Prisma migrate
-- This file serves as documentation and can be used for manual setup if needed
